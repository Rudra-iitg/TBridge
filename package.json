{
  "name": "t-bridge",
  "version": "2.0.0",
  "private": true,
  "description": "Collaborative terminal workspace — peer-to-peer remote execution with a modern TUI.",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @tbridge/protocol && npm run build --workspace @tbridge/crypto && npm run build --workspace @tbridge/engine && npm run build --workspace @tbridge/tui && npm run build --workspace @tbridge/app",
    "dev": "npm run dev --workspace @tbridge/app",
    "dev:relay": "npm run dev --workspace @t-bridge/server",
    "test": "vitest run",
    "typecheck": "npm run typecheck --workspace @tbridge/protocol && npm run typecheck --workspace @tbridge/engine && npm run typecheck --workspace @tbridge/tui && npm run typecheck --workspace @tbridge/app",
    "postinstall": "node scripts/fix-node-pty-permissions.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "node-pty": "^1.2.0-beta.12"
  }
}
